import React, { Component,useState, createContext } from 'react';

export const FlickrContext = createContext();

export const FlickrProvider = (props)=>{
    const [updatedUsername, setPhotos] = useState(null);
}
    